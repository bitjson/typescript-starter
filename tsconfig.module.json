{
  "extends": "./tsconfig",
  "compilerOptions": {
    "target": "esnext",
    "outDir": "build/module",
    "module": "ESNext"
  },
  "exclude": [
    "node_modules/**",
    // typescript-starter.js: exclude CLI from module build, since it's exclusively
    // for Node.js. (This should also be stripped from the generated project.)
    "src/cli/**/*.ts"
  ]
}
